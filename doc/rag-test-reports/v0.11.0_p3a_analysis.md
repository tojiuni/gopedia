# P3-A 분석 리포트 — secondary qrel top-k rank 진단

**기준 run_id**: `6f268af9-ba52-49c1-a31c-848f2c9d0f30` (v0.11.0)  
**분석 날짜**: 2026-05-04  
**엔드포인트**: `GET /runs/{run_id}/queries` (P3-A, gardener_gopedia #26)  
**데이터셋**: `sample_osteon_guide_30_v3` (쿼리 30개, qrel 45개 = primary 30 + secondary 15)

---

## 목적

P@3 = 0.367 개선 방향 결정:
- secondary qrel이 top-k 후보에서 아예 탈락하는지 (`IMP-17` / `P3-D` 필요)
- 후보에 있지만 rank 4-5로 밀리는지 (cross-encoder fine-tuning 필요)

---

## 분석 결과

### secondary qrel rank 분포

| 구분 | 쿼리 수 | secondary 수 | 비율 |
|------|---------|-------------|------|
| top-k 안에 있음 (rank 1-5) | 9 | 9 | 60% |
| top-k 밖 — 완전 탈락 (MISS) | 6 | 6 | 40% |

### MISS 6개 — 벡터 검색에서 탈락한 secondary qrel

| 쿼리 | 1st qrel rank | 2nd qrel rank | Recall@5 |
|------|--------------|--------------|---------|
| Storage 역할 호스트네임 (00 Overview, ost-stor-01) | 1 | **MISS** | 0.5 |
| Kubernetes 설치 방식 (kubeadm, cloud-provider-openstack) | **MISS** | 1 | 0.5 |
| Floating IP 풀 주소 범위 | 1 | **MISS** | 0.5 |
| kolla-ansible 설정 파일 경로 | **MISS** | 1 | 0.5 |
| k8s-subnet CIDR (05 Kubernetes) | 2 | **MISS** | 0.5 |
| clouds.yaml cloud 키 이름 | 1 | **MISS** | 0.5 |

> **공통 패턴**: 동일한 사실(fact)이 여러 문서 섹션에 분산 기록됨.  
> 쿼리 힌트("00 Overview", "05 Kubernetes")가 primary 위치를 명시하지만,  
> secondary는 다른 파일(예: 01 Hardware, 실제 설정 파일)에 같은 내용이 존재.  
> dense 벡터(BGE-M3)는 semantic 유사도 기반 → 정확히 같은 사실의 두 번째 등장 문서를 놓침.

### IN top-k 9개 — 이미 결과에 포함된 secondary qrel

| 쿼리 | qrel ranks | 상태 |
|------|-----------|------|
| OpenStack 릴리스 코드네임 | 1, **2** | top-3 ✅ |
| Deploy 노드 호스트네임 | 1, **1** | top-3 ✅ |
| IaC 도구 | 1, **2** | top-3 ✅ |
| kolla-ansible pip 버전 | 2, **2** | top-3 ✅ |
| K8s 네트워크 이름 | 1, **1** | top-3 ✅ |
| K8s keypair 이름 | 2, **2** | top-3 ✅ |
| IaC 루트 폴더 | 1, **1** | top-3 ✅ |
| /etc/hosts ost-deploy 예시 | 1, **1** | top-3 ✅ |
| Calico IPPool CIDR | 1, **2** | top-3 ✅ |

> 이미 top-k 안에 있는 9개는 대부분 rank 1 또는 2로, P@3에 기여하고 있음.

---

## 결론

### P@3 = 0.367 원인 최종 확정

```
secondary qrel 15개:
  top-k 안 (기여 가능): 9개 → 이미 rank 1-2, P@3 기여 중
  top-k 밖 (완전 탈락): 6개 → 벡터 검색 단계에서 누락

P@3 개선 여지:
  현재: 9/15 secondary가 top-k 안에 있어 P@3 기여
  목표: MISS 6개를 top-k 안으로 끌어올리면 P@3 추가 향상 가능
```

### IMP-17 vs P3-D 방향 결정

| 접근 | 효과 예상 | 근거 |
|------|---------|------|
| **P3-D (BM25 하이브리드)** | **높음** — MISS 원인이 exact keyword match 실패 | 호스트명, CIDR, 파일 경로 등 정확한 문자열은 BM25가 dense보다 강함 |
| IMP-17 (청킹 개선) | 보통 — 청킹 경계 문제라면 효과 있음 | 분석만으로는 청킹 경계가 원인인지 불명확 |

**결정**: `P3-D (하이브리드 검색)` 우선 진행.

- 기술적 근거: MISS 쿼리들은 "ost-stor-01", "/etc/kolla", "10.0.0.0/24" 등 정확한 키워드를 포함. BGE-M3 dense 벡터는 semantic 유사도만 계산하므로, 다른 문서 섹션에 있는 동일 키워드 청크를 놓칠 수 있음.
- BM25 sparse 벡터를 dense와 함께 사용하면 exact keyword match가 보완됨.
- IMP-17은 P3-D 이후 잔여 MISS를 분석하여 필요 시 진행.

---

## 다음 단계

| 항목 | 작업 | 관련 파일 | re-ingest |
|------|------|----------|-----------|
| **P3-D** | Qdrant sparse vector 인덱스 추가 + retriever 하이브리드 검색 | `retriever.py`, ingest 파이프라인, `gopedia-svc.yaml` | **필요** |
| (선택) IMP-17 | P3-D 이후 잔여 MISS 청킹 원인 분석 → heading+fixed-size 하이브리드 | `internal/phloem/chunker/` | **필요** |
